# Minimal environment examples actually used by the stub server
# Copy to .env and adjust as needed

# Port the Express server listens on (overrides config.port)
PORT=3000

# Optional path to alternate config file (JSON) relative or absolute
# CONFIG=./config/local.json

# Enable file watch hot-reload of stub rules (same as passing --watch to start.sh)
# WATCH_RULES=1

# (Add any custom variables your own forked code consumes below)
