{
  "name": "ios-stub-server",
  "version": "1.3.0",
  "description": "Minimal config-driven local stub server (per-endpoint rule folders) with JWT OAuth2 tokens",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [
    "stub-server",
    "mock",
    "local",
    "api",
    "configurable",
    "oauth2",
    "jwt"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "path-to-regexp": "^6.2.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
